## Unreleased

- Fix sample Standard shipping fulfillment option total in `examples.agentic_checkout.json` so the amount matches the subtotal
  and tax, keeping the example coherent.
- Clarify the webhook refund schema by documenting the currency units and aligning the request example to use an integer amount.
