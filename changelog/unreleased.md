# Unreleased

## Fixed

- Corrected the `total` for the standard fulfillment option in `examples.agentic_checkout.json` so the sample data reflects the actual $100 charge instead of an unrealistic `100000000000` value.

